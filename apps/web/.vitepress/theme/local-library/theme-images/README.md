# Optional Theme Images

- Keep one image per preset and use a lowercase kebab-case filename.
- Prefer a 3:2 or 16:9 image at 1600px wide or larger.
- Add the import and matching preset entry in `../theme-image-presets.ts`.
- Theme images must be generic visual material and must not depend on a book's source directory.
